{
  "name": "openboot",
  "version": "0.2.13",
  "description": "One config for every AI coding tool. One command to start your whole stack.",
  "author": "Treadie, Inc.",
  "license": "FSL-1.1-MIT",
  "bin": {
    "boot": "./dist/cli.js"
  },
  "files": [
    "dist",
    "schema.json"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
